<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/new favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content="Astro.generator" />
    <title>TsubasaKoshide website</title>

    <!-- Google Fonts から Noto Sans をインポート -->
<link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP&display=swap" rel="stylesheet">

    
    
    <!-- 共通のスタイルシートを読み込む -->
    <link rel="stylesheet" href="/css/style.css">

  </head>
  <body>
    <header>
        <style>
      /* ボディの背景色を設定 */
      body {
        background-color:#F6F6F6; /* 背景色を#F6F6F6（淡い黄色）に設定 */
      }
          <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="style.css">/* フォント設定 */
    </style>
       <h1 class="noto-sans-jp-header">Tsubasa Koshide</h1>
      <nav>
      <a href="/">Home</a>
      <a href="/about/">About</a>
      <a href="/contact/">Contact</a>
      <a href="https://takstudio.base.shop/" target="_blank" rel="noopener noreferrer">Shop</a>
      <a href="https://takstudio.tokyo/" target="_blank" rel="noopener noreferrer">TAK Studio</a>

      </nav>
    </header>

    <main>
---
import ProjectList from "../components/ProjectList.astro";
---
<html>
  <body>
---
import MainImage from "../assets/projects/project01/main.jpeg";
---
<img src={MainImage} alt="Project 01" width="600" height="400" />

<html>

---
import { getCollection } from "astro:content";
import ProjectCard from "../components/ProjectCard.astro";
import MainImage1 from "../assets/projects/project01/main.jpg";
import MainImage2 from "../assets/projects/project02/main.jpg";
import MainImage3 from "../assets/projects/project03/main.jpg";

const projects = await getCollection("projects");
---
<html>
  <body>
    <h1>My Projects</h1>
    <div class="project-list">
      {projects.map((project, index) => (
        <ProjectCard
          title={project.data.title}
          slug={project.data.slug}
          description={project.data.description}
          thumbnail={index === 0 ? MainImage1 : index === 1 ? MainImage2 : MainImage3}
        />
      ))}
    </div>
  </body>
</html>

<style>
  .project-list {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); /* 3列レイアウト */
    gap: 20px;
  }
</style>
</style>
</html>
