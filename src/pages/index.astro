---
console.log("✅ Astro script is running!");

// `images` をフロントエンドでも使えるように `export` する
export const images = import.meta.glob("../src/assets/projects/*/01.jpg", { eager: true });

console.log("🖼 Loaded images (Server):", images);
console.log("🗝 Image keys:", Object.keys(images));

Object.values(images).forEach((img, index) => {
  console.log(`🔹 Image ${index}:`, img);
  console.log(`🌟 Image ${index} default:`, img?.default);
});
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/new favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content="Astro.generator" />
    <title>TsubasaKoshide website</title>

    <!-- Google Fonts から Noto Sans をインポート -->
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP&display=swap" rel="stylesheet">
    
    <!-- 共通のスタイルシートを読み込む -->
    <link rel="stylesheet" href="/css/style.css">

    <style>
      /* ボディの背景色を設定 */
      body {
        background-color:#F6F6F6; /* 背景色を#F6F6F6（淡い黄色）に設定 */
      }

      /* グリッドレイアウトのスタイル */
      .grid-container {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 16px;
        padding: 20px;
      }
      .grid-container img {
        width: 100%;
        height: auto;
        border-radius: 8px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      }
    </style>
  </head>

    <header>
      <h1 class="noto-sans-jp-header">Tsubasa Koshide</h1>
      <nav>
        <a href="/">Home</a>
        <a href="/about/">About</a>
        <a href="/contact/">Contact</a>
        <a href="https://takstudio.base.shop/" target="_blank" rel="noopener noreferrer">Shop</a>
        <a href="https://takstudio.tokyo/" target="_blank" rel="noopener noreferrer">TAK Studio</a>
      </nav>
    </header>

    <main>
      <div class="grid-container">
        {Object.values(images).map((img, index) => (
          <div key={index}>
            <p>Image Path: {img.default}</p>  <!-- 画像のパスを表示 -->
            <img src={img.default} alt="Project Image">
          </div>
        ))}
      </div>
    </main>
<script>
  // `Astro.props.images` をフロントエンドで取得
  const images = {Astro.props.images};
  console.log("🖼 Client-side Loaded images:", images);
</script>
  </body>
</html>
